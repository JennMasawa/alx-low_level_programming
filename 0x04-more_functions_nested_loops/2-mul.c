#include "main.h"

/**
 * mul - multiplies two integers
 * @a: the first parameter to multiply
 * @b: the second parameter to multiply with the 1st one
 * Return: results of multiplication betwen x and y
 */

int mul(int a, int b)
{
	return (a * b);
}
